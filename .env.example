# ── Media Library Environment Configuration ──────────────────────
# Copy this file to .env and uncomment/set the values you need.
#   cp .env.example .env

# ── API Keys ─────────────────────────────────────────────────────

# TMDB API Key (free at https://www.themoviedb.org/settings/api)
# Sign up for a free account, then request a Developer API key.
# Used for movie/TV metadata, posters, and backdrops.
# TMDB_API_KEY=your_tmdb_api_key_here

# AcoustID API Key (free at https://acoustid.org/new-application)
# Used for audio fingerprint identification of ripped CDs.
# Falls back to name-based MusicBrainz search if not set.
# ACOUSTID_API_KEY=your_acoustid_api_key_here

# Jellyfin API Key (if integration is enabled in config.json)
# JELLYFIN_API_KEY=your_jellyfin_api_key_here

# ── Authentication ───────────────────────────────────────────────

# Flask secret key for session signing.
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
# If not set, a random key is generated on each restart (invalidates sessions).
# FLASK_SECRET_KEY=

# Initial admin account (only used on first run when the DB is empty).
# INIT_ADMIN_USER=admin
# INIT_ADMIN_PASS=changeme

# ── Paths ────────────────────────────────────────────────────────

# Media root directory — all library data lives under this path.
# (database, metadata, thumbnails, movies/, music/, uploads, etc.)
# MEDIA_ROOT=/Users/poppemacmini/Media

# ── Network / Security ──────────────────────────────────────────

# CORS allowed origins (comma-separated). Leave blank for permissive local dev.
# For production set to your domain, e.g.: https://media.yourdomain.com
# CORS_ALLOWED_ORIGINS=

# Set to "true" when serving over HTTPS (e.g. behind Cloudflare Tunnel)
# SECURE_COOKIES=false

# ── Logging ──────────────────────────────────────────────────────

# Override log level (DEBUG, INFO, WARNING, ERROR). Default: INFO
# LOG_LEVEL=INFO

# Allow Werkzeug dev server in Flask-SocketIO (set automatically in Docker).
# Not needed for local development.
# ALLOW_UNSAFE_WERKZEUG=false
