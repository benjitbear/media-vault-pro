# Core dependencies
flask>=3.0.0
flask-socketio>=5.3.0
simple-websocket>=0.10.0
requests>=2.31.0
Pillow>=10.2.0
python-dotenv>=1.0.0
werkzeug>=3.0.0
guessit>=3.8.0

# Audio fingerprinting (optional — falls back to name-based search if missing)
pyacoustid>=1.3.0

# Content ingestion (optional — features degrade gracefully if missing)
yt-dlp>=2024.1.0
trafilatura>=1.8.0
feedparser>=6.0.0
